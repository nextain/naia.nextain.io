# Development Cycle — naia.nextain.io
# Read this BEFORE any coding task.

cycle:
  - phase: PLAN
    actions:
      - Clarify requirements with user
      - Identify affected files
      - Check existing code for duplicates
    output: "Clear scope and file list"

  - phase: CHECK
    actions:
      - Search codebase for related patterns
      - Read relevant files before modifying
      - Verify no conflicting changes
    output: "Understanding of current state"

  - phase: BUILD
    approach: TDD
    actions:
      - "RED: Write failing test for the feature/fix"
      - "GREEN: Write minimal code to pass"
      - "REFACTOR: Clean up while tests stay green"
    rules:
      - Never code without test first
      - Integration tests preferred over unit tests
      - No console.log — use structured patterns

  - phase: VERIFY
    actions:
      - "Check .env for required test data (API keys, tokens, URLs) — if missing, ask user"
      - "Run targeted tests FIRST (changed scope only) for fast feedback"
      - "Run full test suite (npm test)"
      - "Run dev server (npm run dev) and verify in browser"
      - "After targeted tests pass, run full E2E suite if available"
    rules:
      - "MUST actually run the web server — type check alone is NOT verification"
      - "Test data comes from .env — if missing, ask user (never guess)"
      - "Changed parts first, then full suite"

  - phase: CLEAN
    actions:
      - Remove unused imports/files
      - Check for duplicate code
      - Self-review all changes

  - phase: COMMIT
    actions:
      - Stage specific files (no git add -A)
      - "Commit: <type>(<scope>): <description>"
      - Update work-logs if significant
